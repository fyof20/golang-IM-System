# golang-IM-System 代码问题清单

> 最近复查：2026-09-15（私聊格式、提示文案、断线清理已修复后）
> 状态标记：✅ 已修复 ｜ ⚠️ 修复不完整（有残留） ｜ ❌ 未修复

---

## 🔴 严重问题

### 1. 私聊功能 ⚠️ 客户端侧已修复，服务器侧残留 1 个防崩问题

**a) `Run()` 没调用私聊** — `client/client.go:151-154` — ✅ 已修复
case 2 现在正确调用了 `this.PrivateChat()`。

**b) 消息格式** — `client/client.go:86` — ✅ 已修复
```go
sendMsg := "to" + "|" + remoteName + "|" + chatMsg + "\n"
```
`|` 分隔和单个 `\n` 都对了，与服务器 `to|张三|内容` 格式完全匹配。

**c) 服务器 panic 风险** — `server/user.go:109` — ❌ 未修复（当前最优先）
```go
content := strings.Split(msg, "|")[2]
```
收到 `to|某在线用户`（只有两段、无内容）时 `[2]` 越界 → **整个 server 崩溃**。
自己的客户端现在不会发这种消息，但任何手连 TCP（telnet/netcat）或异常客户端仍可触发。
取值前需检查：`parts := strings.Split(msg, "|"); len(parts) < 3` 就报错返回。

### 2. `PrivateChat()` 死循环 — `client/client.go:84-97` — ✅ 已修复

内层循环第 96 行已改为 `fmt.Scanln(&chatMsg)`，死循环消除；
第 95 行提示语也已改为"请输入聊天内容，exit退出"，与实际读取的变量一致，流程正确。

### 3. 广播死锁风险 — `server/server.go:39-44` — ❌ 未修复

```go
this.mapLock.Lock()
for _, cli := range this.OnlineMap {
    cli.C <- msg        // 无缓冲 channel，阻塞式发送
}
this.mapLock.Unlock()
```
只要有一个客户端不读数据（TCP 窗口满 → 它的 `conn.Write` 阻塞 → 它的 goroutine
卡在 `C` 上），`ListenMessage` 就持着写锁卡死 → 所有用户的 `who`/`rename`/上下线
全部卡死，**整个服务器冻结**。

改进方向：给 `user.C` 加缓冲（如 `make(chan string, 500)`），或发送改为
`select + default` 丢弃过慢用户的消息。

### 4. rename 的数据竞争 — `server/user.go:81` — ❌ 未修复

```go
_, ok := this.server.OnlineMap[newName]   // ← 没加锁就读 map
```
两个连接同时改成同一个名字，都可能通过检查，后一个覆盖前一个；
`server/user.go:90` 的 `this.Name = newName` 在解锁后执行也有竞争。
把整个 rename 分支放进 `mapLock` 里。`go run -race` 可直接抓到。

---

## 🟡 中等问题

### 5. 读 goroutine 泄漏的边角情况 — `server/server.go:65-87` — ✅ 已修复

读 goroutine 现在所有退出路径（`n==0`、非 EOF 错误）都统一 `break` 出循环、
发送 `isLive <- false` 通知 Handler，不再有不清理就退出的路径。

### 6. 下线二次广播 / Handler 空等 5 分钟 — `server/server.go:90-122` — ✅ 已修复

select 通过 `live == false` 分支感知断线，立即执行和超时踢人同一套清理
（`Offline()` → `close(user.C)` → `conn.Close()`，顺序正确），不再空等 5 分钟、
不再二次广播"下线"、不再泄漏 goroutine 和文件描述符。

### 7. 客户端输入非数字直接退出 — `client/client.go:50` — ❌ 未修复

`fmt.Scanln(&flag)` 遇到字母返回 error，`flag` 保持 0 → `Run()` 的
`for this.flag != 0` 直接退出。手滑敲一个字母客户端就关闭了。
应在 `Scanln` 出错时清空输入、让 `menu()` 返回 false 重试。

---

## 🟢 小问题

### 8. TCP 粘包/半包假设 — `server/server.go:78` — ❌ 未修复（教程简化，可接受）

`msg := string(buf[:n-1])` 假设每次 Read 恰好读到一整条以 `\n` 结尾的消息。
TCP 粘包/半包时会切错消息。生产上应按 `\n` 做消息边界切分（如 `bufio.Scanner`）。

### 9. 命名拼写 — ❌ 未修

| 位置 | 现状 | 应为 |
|---|---|---|
| `client/client.go:125` | `UpadateName` | `UpdateName` |
| `client/client.go:129` | `snedMsg` | `sendMsg` |
| `server/user.go:10` | `Adder` | `Addr` |
| `client/client.go:166` | `SeverPort` | `ServerPort` |
| `server/user.go:105` | `改用户名不存在` | `该用户名不存在` |

### 10. 可执行文件不该入库 — ❌ 未处理

`client/client.exe`、`server/server.exe`（约 7MB）应加入 `.gitignore`。

---

## 当前进度总结

| # | 问题 | 状态 |
|---|---|---|
| 1 | 私聊不可用（3 个子问题） | ⚠️ 客户端侧已修好，剩 1c 服务器越界保护 |
| 2 | PrivateChat 死循环 | ✅ |
| 3 | 广播持锁阻塞死锁 | ❌ |
| 4 | rename 数据竞争 | ❌ |
| 5 | 读 goroutine 泄漏边角 | ✅ |
| 6 | 下线二次广播/空等 5 分钟 | ✅ |
| 7 | 非数字输入退出客户端 | ❌ |
| 8 | TCP 粘包假设 | ❌（可接受） |
| 9 | 拼写 | ❌ |
| 10 | exe 入库 | ❌ |

下一步优先修：**问题 1c 越界保护**（一行判断防服务器崩溃）→
**问题 4 rename 加锁**（`go run -race` 验证）→ **问题 3 广播加缓冲或 select+default**。
